В файле RefList.php нужно поправить переменные $user и $pass подставив актуальные значения для локального хоста.
Необходимо создать базу данных под названием cutter и в ней таблицу refs, которая должна содержать столбцы id, original и cutted.